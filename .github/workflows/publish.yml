name: Publish images

# Builds the api and web Docker images and pushes them to GitHub Container
# Registry on every push to main. The production server only ever `docker
# compose pull` these — it never runs `npm install`/`tsc`/`vite build`
# itself, which matters on a 1GB droplet with no swap.
on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: api
            dockerfile: apps/api/Dockerfile
          - name: web
            dockerfile: apps/web/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/resort-finance-tracker-${{ matrix.name }}:latest
            ghcr.io/${{ github.repository_owner }}/resort-finance-tracker-${{ matrix.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
